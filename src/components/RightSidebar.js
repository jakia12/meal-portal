import React from 'react'

const RightSidebar = () => {
    return (
        <div>
            <button className="btn btn-outline btn-primary">Sign in with FaceBook</button>
            <button className="btn btn-outline btn-secondary my-6">Sign in with Twitter</button>

        </div>
    )
}

export default RightSidebar
